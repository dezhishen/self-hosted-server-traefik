api_version: "1.0"
name: mariadb
description: "MariaDB database server"
category: database
tags: [mariadb, mysql, database, sql]
dependencies: []

params:
  - name: "app.MYSQL_PASSWORD"
    type: password
    label: "Mysql Password"
    secret: true

container:
  name: mariadb
  image: mariadb:11.3.2
  user: "{{ index .Custom "common.PUID" | default \"1000\" }}:{{ index .Custom "common.PGID" | default \"1000\" }}"
  volumes:
    - "{{ .DataDir }}/mariadb/data:/var/lib/mysql:Z"
  environment:
    TZ: "{{ index .Custom "common.TZ" | default \"Asia/Shanghai\" }}"
    LANG: "zh_CN.UTF-8"
    MARIADB_USER: root
    MARIADB_ROOT_PASSWORD: "{{ index .Custom "app.MYSQL_PASSWORD" }}"
  networks:
    - "{{ .NetworkName }}"
  restart: always
  resources:
    memory: "512M"
